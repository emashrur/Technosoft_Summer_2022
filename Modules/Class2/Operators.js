/**
 * Operators in JS:
 * 
 * 1. Assignment Operators (=)
 * 2. Arithmetic (+, -, *, /, %, ++, --)
 * 3. Comparison Operator (===, >=, <=, >, <, !==)
 * 4. Logical (&&, ||, !)
 * 5. Ternary (?:)
 * 6. Advanced Assignement (+=, -=, *=, /=, %=)
 */

 let lVar1=10;
 let lVar2=20;
 let lVar3=30;
 let lVar4=40;
 
 let res1 = lVar1 + lVar2++;
 console.log(`lVar1 = ${lVar1}`);
 console.log(`lVar2 = ${lVar2}`);
 console.log(`res1 = ${res1}`);
 
